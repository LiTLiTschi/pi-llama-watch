{
  "name": "pi-llama-watch",
  "version": "0.1.0",
  "description": "Pi extension showing LLM slot status in status bar",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "keywords": ["pi-package", "pi", "extension", "llama", "watch"],
  "author": "LiTLiTschi",
  "license": "MIT",
  "pi": {
    "extensions": ["./src"]
  }
}
